#include <stdio.h>

int main()
{
float l, b, p;
 
 printf(" We are going to find the Perimeter of a rectangle. \n Enter length and breadth of the rectangle\n");
 scanf("%f%f", &l, &b);
 
 p = 2*l + 2*b;
 
 printf("Perimeter of the rectangle = %f\n", p);
 
 return 0;

}
